version: '3'
services:
  yeehack:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    privileged: true
    restart: unless-stopped
    volumes:
      - '/var/run/dbus:/run/dbus:ro'
